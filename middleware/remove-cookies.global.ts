/**
 * Client-side middleware (empty for now)
 * Cookie removal is handled by server middleware and .htaccess
 */
export default defineNuxtRouteMiddleware(() => {
  // No-op for client-side
  // Cookie removal happens on server side
})
