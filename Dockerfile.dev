# Dockerfile pour le développement (Nuxt 3)
FROM node:20-alpine

WORKDIR /app

# Exposer le port
EXPOSE 3000

# Variables d'environnement pour le dev
ENV NODE_ENV=development
ENV HOST=0.0.0.0
ENV PORT=3000

# Installer les dépendances au démarrage et lancer en mode dev
# Les fichiers seront montés via le volume docker-compose
CMD ["sh", "-c", "npm install && npm run dev"]
